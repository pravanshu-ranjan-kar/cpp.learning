#include <iostream>
using namespace std;
int main() {
   int a,b;
   cout<<"enter the values of a and b";
   cin>>a>>b;
   cout<<"is value of a and b less than 30? "<<(a<30&&b<30);
   cout<<"is value of a or b less than 15?"<<(a<15||b<15);
   
    return 0;
}
